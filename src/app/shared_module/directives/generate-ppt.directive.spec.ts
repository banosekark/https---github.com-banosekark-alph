import { GeneratePptDirective } from './generate-ppt.directive';

describe('GeneratePptDirective', () => {
  it('should create an instance', () => {
    const directive = new GeneratePptDirective();
    expect(directive).toBeTruthy();
  });
});
